diff --git a/drivers/boiler/device.ts b/drivers/boiler/device.ts
index 89dfe20..ca8b919 100644
--- a/drivers/boiler/device.ts
+++ b/drivers/boiler/device.ts
@@ -130,6 +130,21 @@ export class BoilerDevice extends Homey.Device {
       await this.client.setBoilerValue("hpin2opt", data).catch(this.error);
     });
 
+    // Flow actions reuse the capability listeners above, so the device's
+    // capability state stays in sync regardless of whether it's toggled
+    // from the UI or from a Flow.
+    this.homey.flow
+      .getActionCard("boiler_set_solar_active")
+      .registerRunListener(async (args: { state: boolean }) => {
+        await this.triggerCapabilityListener("boiler_hpin4opt", args.state);
+      });
+
+    this.homey.flow
+      .getActionCard("boiler_set_block_heatpump")
+      .registerRunListener(async (args: { state: boolean }) => {
+        await this.triggerCapabilityListener("boiler_hpin2opt", args.state);
+      });
+
     this.startPolling();
   }
 
diff --git a/drivers/boiler/driver.flow.compose.json b/drivers/boiler/driver.flow.compose.json
index 8454c16..442d9e7 100644
--- a/drivers/boiler/driver.flow.compose.json
+++ b/drivers/boiler/driver.flow.compose.json
@@ -52,5 +52,49 @@
         }
       ]
     }
+  ],
+  "actions": [
+    {
+      "id": "boiler_set_solar_active",
+      "title": {
+        "en": "Set Solar Active",
+        "nl": "Stel Zon actief in"
+      },
+      "titleFormatted": {
+        "en": "Set Solar Active to [[state]]",
+        "nl": "Stel Zon actief in op [[state]]"
+      },
+      "args": [
+        {
+          "type": "checkbox",
+          "name": "state",
+          "title": {
+            "en": "On",
+            "nl": "Aan"
+          }
+        }
+      ]
+    },
+    {
+      "id": "boiler_set_block_heatpump",
+      "title": {
+        "en": "Set Block Heatpump",
+        "nl": "Stel Warmtepomp blokkeren in"
+      },
+      "titleFormatted": {
+        "en": "Set Block Heatpump to [[state]]",
+        "nl": "Stel Warmtepomp blokkeren in op [[state]]"
+      },
+      "args": [
+        {
+          "type": "checkbox",
+          "name": "state",
+          "title": {
+            "en": "On",
+            "nl": "Aan"
+          }
+        }
+      ]
+    }
   ]
 }
